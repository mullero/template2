# Dev/test dependencies. Kept separate from requirements.txt to avoid pip
# resolver conflicts in the runtime image.
-r requirements.txt

pytest>=8.3,<9
pytest-asyncio>=0.24,<0.25
pytest-cov>=6.0,<7
pytest-mock>=3.14,<4
pytest-xdist>=3.6,<4
faker>=30,<31
coverage>=7.6,<8

ruff>=0.7,<0.8
mypy>=1.13,<2
